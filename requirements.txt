fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
python-dotenv==1.0.1
jinja2==3.1.5
httpx==0.28.1
openai==1.58.1
python-multipart==0.0.20
websockets==14.1
# Ingestion documenti (condominio): PyMuPDF = conteggio pagine PDF;
# pandas+openpyxl = import inquilini da Excel. (pdftotext = poppler-utils, vedi Dockerfile)
PyMuPDF==1.24.14
pandas==2.2.3
openpyxl==3.1.5
# MCP server per agenti vocali esterni (es. ElevenLabs). starlette pinnato per
# compatibilità con fastapi 0.115.6 (mcp tenderebbe ad aggiornarlo).
mcp==1.28.0
starlette==0.41.3
# Driver Postgres (per puntare il DB a Supabase invece che a SQLite)
psycopg2-binary==2.9.9
